<link rel="stylesheet/components" href="_card.scss" />

<section class="accueil-photos">
    <% @categories.each do |category| %>
      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3))">
        <div class="card-image">
        <a href="<%= admin_category_path(category) %>">
          <% if category.cover.attached? %>
            <%= cloudinary_image_tag category.cover, alt: category.name, height: 300, width: 400, crop: :fill %>
          <% else %>
            <div>No Cover Image</div>
          <% end %>
        <div class="card-category-params">
          <%= link_to raw('<i class="fa-regular fa-pen-to-square"></i>'), edit_admin_category_path(category), class: 'btn btn-icon' %>

          <%= form_with(model: category, url: admin_category_path(category), method: :delete, data: { turbo_confirm: 'Are you sure?' }) do |form| %>
            <%= form.button type: :submit, class: 'btn btn-icon' do %>
              <i class="fa-solid fa-trash"></i>
            <% end %>
          <% end %>
        </div>
        </div>
          <div class="card-category-text"><%= link_to category.name.capitalize, admin_category_path(category)%></div>
        </a>
      </div>
    <% end %>
</section>

<div class="create">
  <%= link_to "Create", new_admin_category_path, class: "btn btn-dark" %>
</div>
